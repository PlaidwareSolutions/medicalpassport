{
  "name": "@medpass/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@medpass/config": "workspace:*",
    "@medpass/database": "workspace:*",
    "@medpass/observability": "workspace:*",
    "bullmq": "^5.34.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
