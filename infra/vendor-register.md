# Vendor & Subprocessor Register

Per docs/18 §Vendor & subprocessor register. Created at first vendor onboarding (Telnyx, this session) — one row per vendor, kept current as each is actually connected. "Not yet onboarded" rows are placeholders for vendors this project anticipates (docs/24 open decisions) but hasn't connected an account for.

| Vendor | Service | Data shared | Region | Contract/DPA status | Retention | Training-use (AI) | Exit strategy |
|---|---|---|---|---|---|---|---|
| Telnyx | SMS (OTP delivery, dose/refill/completion/escalation reminder text) + inbound delivery-status webhooks | Phone number (E.164) + message text (never the OTP code's context, never the medicine name unless the patient opted into `full_name` wording); inbound webhook carries the message id + delivery status/error code, verified via Telnyx's Ed25519 signature before use | US (account checked this session has one US toll-free number; no other region configured) | Not yet reviewed — **Requires legal review** before production use (DPA status unconfirmed) | Message content/metadata retention governed by Telnyx's own policy, not this app's retention crons; this app stores only the delivery attempt's status/error digest, never the message text, in `notification_attempts` | Not applicable — SMS is not an AI/ML service | Swappable: both `OtpSender` and `SmsMessageSender` are interfaces (`@medpass/notifications`); a different vendor is a new adapter class, no call-site changes. Webhook receiver (`TelnyxWebhookController`) is Telnyx-specific (signature scheme isn't standardized across SMS vendors) — a vendor swap would need a new webhook handler, not just a new adapter |
| Railway | Compute + PostgreSQL hosting | All application data — real data now, since `medpass-dev` is live (docs/24 OD-5 Singapore assumption, confirmed as the actual region: `asia-southeast1-eqsg3a`) | Singapore | Not yet reviewed — **Requires legal review** before production use (DPA status unconfirmed) | Governed by Railway's own retention, not yet a documented app-level policy for this dev/staging environment | N/A | Documented in docs/25 (Railway deployment architecture); IaC lives in `.railway/railway.ts` |
| Cloudflare | Domain registrar (`medidocs.app`) + edge (DNS, TLS, WAF managed ruleset, one custom rate-limit rule, cache rules) now live in front of Railway; R2 object storage and Turnstile not yet connected | Edge traffic metadata (IP, host, path) for the three `staging-*.medidocs.app` hostnames | Global (Cloudflare anycast edge) | Not yet reviewed — **Requires legal review** before production use (DPA status unconfirmed) | Governed by Cloudflare's own edge log retention, no app-level policy yet | N/A | DNS/WAF/cache config is API-token-driven (not IaC-tracked); local-disk stand-in still in use instead of R2 (ADR-12) — R2 swap is a config change to the same `ObjectStorage` interface. Connected API token has DNS/WAF/Cache Rules/Zone Settings scope only — no Turnstile or R2/Workers scope yet |
| WhatsApp BSP (OD-10) | WhatsApp reminder delivery | Not yet onboarded | — | — | — | — | — |
| OCR provider (OD-11) | Handwriting/Indic-script OCR | Not yet onboarded (Tesseract.js runs locally instead — no vendor, no data leaves the app) | — | — | — | — | — |
| AI provider (OD-12) | Content simplification/translation | Not yet onboarded | — | — | — | — | — |
| Licensed drug DB (OD-3/4) | Interaction/contraindication data | Not yet onboarded | — | — | — | — | — |
