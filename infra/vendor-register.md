# Vendor & Subprocessor Register

Per docs/18 §Vendor & subprocessor register. Created at first vendor onboarding (Telnyx, this session) — one row per vendor, kept current as each is actually connected. "Not yet onboarded" rows are placeholders for vendors this project anticipates (docs/24 open decisions) but hasn't connected an account for.

| Vendor | Service | Data shared | Region | Contract/DPA status | Retention | Training-use (AI) | Exit strategy |
|---|---|---|---|---|---|---|---|
| Telnyx | SMS (OTP delivery, dose/refill/completion/escalation reminder text) + voice-call OTP (Call Control, supplementary channel) + inbound delivery-status/call-event webhooks | Phone number (E.164) + message text/spoken code (never the OTP code's context, never the medicine name unless the patient opted into `full_name` wording); inbound webhooks carry message/call ids + status, verified via Telnyx's Ed25519 signature before use | US (one toll-free number, now assigned to both a messaging profile and a Call Control connection); voice's outbound destination whitelist additionally includes India (a new, medpass-specific outbound voice profile — the account's shared "Default" profile, US/CA-only, was left untouched) | Not yet reviewed — **Requires legal review** before production use (DPA status unconfirmed) | Message/call content retention governed by Telnyx's own policy, not this app's retention crons; this app stores only the delivery attempt's status/error digest, never the message text or call audio, in `notification_attempts` | Not applicable — neither SMS nor Call Control TTS is an AI/ML service | Swappable: `OtpSender`/`SmsMessageSender` are interfaces (`@medpass/notifications`); a different vendor is a new adapter class, no call-site changes. Both webhook receivers (`TelnyxWebhookController`, `TelnyxVoiceWebhookController`) are Telnyx-specific (signature scheme isn't standardized across vendors) — a vendor swap would need new webhook handlers, not just new adapters. Voice OTP is a real, live-verified supplementary channel, **not** a confirmed way around India's SMS DLT requirement (TRAI's rules also cover automated voice/IVR) |
| Railway | Compute + PostgreSQL hosting | All application data — real data now, since `medpass-dev` is live (docs/24 OD-5 Singapore assumption, confirmed as the actual region: `asia-southeast1-eqsg3a`) | Singapore | Not yet reviewed — **Requires legal review** before production use (DPA status unconfirmed) | Governed by Railway's own retention, not yet a documented app-level policy for this dev/staging environment | N/A | Documented in docs/25 (Railway deployment architecture); IaC lives in `.railway/railway.ts` |
| Cloudflare | Domain registrar (`medidocs.app`) + edge (DNS, TLS, WAF managed ruleset, one custom rate-limit rule, cache rules) live in front of Railway; R2 object storage now live too (5 buckets: `medpass-dev-{patient-docs,derived,ocr-tmp,backups,public-assets}`); Turnstile not yet connected | Edge traffic metadata (IP, host, path) for the three `staging-*.medidocs.app` hostnames; prescription document files (encrypted at rest in R2) | Global (Cloudflare anycast edge); R2 buckets location-hinted `apac` | Not yet reviewed — **Requires legal review** before production use (DPA status unconfirmed) | Governed by Cloudflare's own edge log retention; R2 has no app-level retention policy yet beyond `ocr-tmp`'s 48h lifecycle rule | N/A | DNS/WAF/cache config is API-token-driven (not IaC-tracked). Two separate API tokens in use, deliberately least-privilege: one broad (DNS/WAF/Cache Rules/Zone Settings/Turnstile/R2-management) used only for provisioning, and one narrow (Workers R2 Storage only) whose id/value-hash *is* the R2 S3 credential embedded in the running api/worker/cron processes — that's the one that would matter if the deployed app were ever compromised. Local-disk stand-in (ADR-12) remains the fallback whenever R2 env vars aren't set (local dev/test) |
| WhatsApp BSP (OD-10) | WhatsApp reminder delivery | Not yet onboarded | — | — | — | — | — |
| OCR provider (OD-11) | Handwriting/Indic-script OCR | Not yet onboarded (Tesseract.js runs locally instead — no vendor, no data leaves the app) | — | — | — | — | — |
| AI provider (OD-12) | Content simplification/translation | Not yet onboarded | — | — | — | — | — |
| Licensed drug DB (OD-3/4) | Interaction/contraindication data | Not yet onboarded | — | — | — | — | — |
